spring:
  datasource:
    password: database-it-non-secret-password
  task:
    scheduling:
      enabled: false

admin:
  bootstrap:
    enabled: false

security:
  jwt:
    secret: database-it-jwt-secret-value-at-least-32-chars
  config:
    encryption-key: database-it-config-encryption-key-32chars

market:
  demo-quotes:
    enabled: false
  test-data:
    enabled: false

execution:
  mode: demo

trading:
  pending-order-execution-enabled: false
  protective-order-execution-enabled: false

trading-lab:
  report:
    cleanup:
      enabled: false
